<Window x:Class="TidyMind.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TidyMind"
        mc:Ignorable="d"
        Title="TidyMind" Height="500" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Background="#1E1E1E">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#252526" Padding="20,14">
            <Grid>
                <TextBlock x:Name="ProfileTitle" Text="TidyMind"
                           FontSize="20" FontWeight="Bold"
                           Foreground="#569CD6"
                           VerticalAlignment="Center"/>
                <Button x:Name="SwitchProfileButton" Content="Switch Memory"
                        Width="130" Height="30" HorizontalAlignment="Right"
                        Click="SwitchProfileButton_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="#1E1E1E" Padding="20,10">
            <StackPanel Orientation="Horizontal">
                <TextBox x:Name="ProjectInput" Width="380" Height="32"
                         VerticalContentAlignment="Center"/>
                <Button x:Name="AddButton" Content="+ Add"
                        Height="32" Margin="10,0,0,0" Padding="14,0"
                        Click="AddButton_Click"/>
                <Button x:Name="EditButton" Visibility="Collapsed" Click="EditButton_Click"/>
                <Button x:Name="DeleteButton" Visibility="Collapsed" Click="DeleteButton_Click"/>
                <TextBlock Text="Right-click a project to edit or delete"
                           FontSize="11" Foreground="#858585"
                           VerticalAlignment="Center" Margin="14,0,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="#1E1E1E" Padding="20,0,20,10">
            <TextBox x:Name="SearchBox" Height="32"
                     VerticalContentAlignment="Center"
                     Background="#3C3C3C" Foreground="#858585"
                     BorderBrush="#474747"
                     Text="Search..."
                     GotFocus="SearchBox_GotFocus"
                     LostFocus="SearchBox_LostFocus"
                     TextChanged="SearchBox_TextChanged"/>
        </Border>

        <ListBox x:Name="ProjectList" Grid.Row="3"
                 Margin="20,12,20,0"
                 DisplayMemberPath="Name"
                 MouseDoubleClick="ProjectList_MouseDoubleClick"
                 PreviewMouseRightButtonUp="ProjectList_PreviewMouseRightButtonUp"/>

        <TextBlock Grid.Row="4" Text="Double-click to open a project"
                   Foreground="#858585" FontSize="11"
                   HorizontalAlignment="Center" Margin="0,8,0,10"/>

        <Button x:Name="QuickNotesButton" Grid.RowSpan="5"
                Style="{StaticResource IconButtonStyle}"
                Content="📝" ToolTip="Quick Notes (Ctrl+Shift+N)"
                Width="36" Height="36" FontSize="16" Padding="0,0,6,0"
                Background="#3C3C3C"
                HorizontalAlignment="Right" VerticalAlignment="Bottom"
                Margin="0,0,16,16" Panel.ZIndex="100"
                Click="QuickNotesButton_Click"/>
    </Grid>
</Window>